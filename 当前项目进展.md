# 简历优化网站当前项目进展

更新日期：2026-09-13  
当前开发分支：`resume-processing`

## 一、当前结论

项目已经从首页视觉原型推进到一条可运行的简历处理闭环：用户上传 PDF 或 DOCX，系统提取并展示事实材料，用户核对后进入 AI 诊断，确认或跳过补充问题，再生成可编辑的优化简历并导出 PDF。

当前已经完成的是“内容提取、事实核对、诊断、优化、编辑、固定版式 PDF 导出”的首轮产品链路。多套简历模板、浏览器本地 OCR 和真实用户样本的大规模效果评估尚未完成，不能把当前版本描述为最终商业化产品。

## 二、已经完成的产品能力

### 1. 首页与上传入口

- 保留已确认的纯黑首页、宋体艺术字和磨砂 OFFER 信封视觉方向。
- 支持选择或拖入 PDF、DOCX 文件。
- 文件类型、文件大小和伪造扩展名均由服务端校验。
- 上传、提取、诊断、优化和导出均使用真实任务状态，不用假进度冒充完成。

### 2. PDF 与 DOCX 内容提取

- DOCX 直接读取段落和表格文字，不做 OCR。
- 文字型 PDF 读取文字及页面位置，并按视觉坐标恢复阅读顺序。
- 已修复 PDF 内部写入顺序导致相邻编号职责正文错位的问题，能够保持第 5、第 6 点标题与各自正文的归属。
- 扫描型或无有效文字层的 PDF 会明确提示当前无法直接提取；浏览器本地 OCR 是已确认方向，但尚未实现。
- 不使用云 OCR，也不会把 PDF 页面图片发送给第三方 OCR 服务。

### 3. 全页材料核对

- 原来的窄弹窗已替换为宽幅二级页面。
- 联系方式使用紧凑单行输入框。
- 实际必填字段显示 `*`，不额外制造无必要的个人必填项。
- 基本资料、教育和工作经历旁不再显示来源选择框。
- 来源原文集中保留在页面末尾，由系统自动维护引用关系。
- 工作内容、技能和来源原文完整展开，不限制在窄小的内部滚动框中。
- 用户新增或修改事实时自动生成用户确认来源，避免要求用户理解 sourceId。

### 4. AI 方法论与诊断

- 《简历 AI 方法论 v0.1》已经由说明文档转为可执行规则注册表，共 16 条稳定规则。
- 生成前先进行诊断，检查完整度、时间、清晰度、能力证据和业务逻辑等问题。
- 每个补充问题都必须提供一条基于现有事实的保守“建议表达”。
- 用户可以采用建议表达、修改后确认，或者跳过问题继续生成保守版本。
- 不再限制只显示 3 个问题；所有确实影响表达的独立问题均应展示。
- 实现层保留 12 条安全上限，用于拒绝重复或失控的模型数组，不会静默截断成前三条。
- 诊断结果中的 D02、F03、E03 等内部规则编号只保留在后端追踪数据中，不向用户展示。

### 5. AI 简历优化

- 支持 OpenAI、DeepSeek 和阿里云通义千问的服务端配置与按顺序降级。
- 浏览器不会接触任何服务商密钥。
- 当前本地已配置并验证 DeepSeek `deepseek-flash`。
- 优化提示词会接收已经校验的诊断结果，必须逐条处理诊断问题，而不是只读取原始材料。
- 对来源不明确的百分比、同比/环比口径、职责边界和成果，AI 不得自行猜测；用户跳过时应省略歧义数字或使用保守表达。
- 服务端校验来源 ID、方法论规则 ID、新增数字、公司、职位、日期、职责升级、内部编号和技能熟练度。
- AI 输出与来源过于相似、没有形成实质优化时会被质量门禁拒绝并重新生成。
- 每段工作经历采用“加粗短标题 + 内容”的结构，便于面试官快速识别候选人做了什么。

### 6. 技能表达规则

- 模块名称固定为“技能”，不显示“专业技能”“专业能力”或“技能清单”。
- 技能条目不显示“经历 01”，也没有额外的条目级大标题。
- 每条技能按单行“小标题：正文”展示并导出，例如：

  > **付费与活动玩法：**具备付费卡点设计、会员体系设计、商品结构规划、抽奖/盲盒/礼包活动设计和活动复盘经验。

- 禁止使用“商业化运营”“用户运营”“产品运营”“工具”“常用工具”“工具使用”“工具能力”等泛化标题。
- 工具类内容按用途改写为“原型与流程梳理”“数据整理与表格处理”等具体能力场景。
- 企业内部 AI 工具、企业自研数据看板等不可迁移内容默认不进入投递稿。
- “精通”“熟练”等熟练度必须由来源或用户回答明确支持。

### 7. 等待与自动重试

- 提取、诊断、优化和导出时显示居中的 loading 蒙层。
- 蒙层包含职业激励文案、真实已等待时间和原创像素闯关到 OFFER 的动画。
- 动画不伪造成功百分比，支持暂停和减少动态效果偏好。
- AI 返回不可解析、结构不合格或没有实质优化的结果时，服务端会在同一个浏览器请求中自动重试。
- 最大调用预算为首次生成加 2 次自动重试，共 3 次；即使配置多个服务商也共享这 3 次预算。
- 重试期间 loading 不关闭，材料不丢失。三次均失败后才显示最终错误。
- 密钥、余额、权限、网络和超时错误不盲目重试，避免无意义等待和重复成本。

### 8. 最终结果与编辑

- 优化结果页与材料核对页使用相同的宽幅页面、顶部栏、目录、内容区和响应式布局。
- 工作经历不使用独立卡片框，只保留标题、正文和轻量分隔。
- 每个目录分区都提供编辑入口，用户无需返回页面顶部。
- 姓名、联系方式、目标岗位、个人概述、模块名称、经历元数据、条目标题和正文都可直接编辑。
- 编辑后的内容无需再次调用 AI，可直接导出。
- “生成说明”已更名为“简历提醒”，只用于当前页面提醒，不进入正式 PDF。

### 9. PDF 导出

- 当前支持生成可下载的中文 PDF。
- PDF 使用结构化简历数据生成，不让模型输出任意 HTML。
- 工作经历按“标题 + 正文”层级导出。
- 技能按“小标题：正文”单行导出。
- 用户在最终页确认的编辑内容会进入 PDF。
- 简历提醒、内部诊断编号和后端追踪信息不会进入 PDF。

## 三、当前技术结构

```text
上传 PDF / DOCX
        ↓
服务端文件校验与文字提取
        ↓
统一事实数据与来源片段
        ↓
用户全页核对和补充
        ↓
AI 诊断 + 建议表达
        ↓
用户采用、修改或跳过问题
        ↓
AI 优化 + 服务端事实与质量校验
        ↓
最终页直接编辑
        ↓
固定版式 PDF 导出
```

## 四、已经完成的验证

- 核心自动化测试：223 项全部通过。
- 独立浏览器交互回归：8 项全部通过。
- Sites 打包测试：4 项全部通过。
- 生产构建通过，并生成客户端、服务端和 hosting 配置文件。
- `git diff --check` 无空白格式错误，仅有 Windows 换行提示。
- 使用合成材料完成 DeepSeek `deepseek-flash` 真实服务验证：生成成功、技能标题符合规则、内部工具被省略、PDF 导出有效。
- 真实服务验证没有上传用户的真实简历，也没有记录服务商凭据。

## 五、尚未完成与不能夸大的部分

### 1. 浏览器本地 OCR

产品方向已经确定：只有扫描 PDF 或缺失文字层的页面才在浏览器本地 OCR，不使用云 OCR。当前尚未接入 OCR 模型，因此扫描型 PDF 仍不是已完成能力。

### 2. 多套简历模板

当前只有结构化结果页和固定版式 PDF。适合运营、通用职能、应届生和英文岗位的多套模板尚未完成。后续模板应消费同一份结构化简历数据，切换模板时不重新提取、不重新优化。

### 3. DOCX 导出

当前可上传 DOCX，但正式结果主要导出 PDF。生成新版 DOCX 尚未完成。

### 4. 真实用户质量评估

已有合成场景、自动化校验和少量真实文件提取验证，但尚未建立多岗位、多人群的脱敏真实简历评测集。当前不能宣称能够保证录用、通过 ATS 或稳定达到某个优化分数。

### 5. 专业岗位方法论

现有方法论通用规则和案例更偏产品、运营、市场。技术、电子信息、AI、财务、建筑等方向仍需要补充岗位专属规则和样本验证。

## 六、建议的下一阶段优先级

1. 建立脱敏真实简历回归集，验证提取正确率、事实忠实度、表达改善和问题有效性。
2. 完成第一套正式简历模板的数据映射和分页规则，再扩展多模板切换。
3. 接入浏览器本地 OCR，只对缺失文字层的异常页面运行。
4. 增加 DOCX 导出，并确保与 PDF 使用同一份结构化内容。
5. 为技术、财务、建筑等岗位补充专业规则，而不是直接套用运营案例。
6. 增加用户可理解的重试状态提示，但不显示虚假进度或底层错误信息。

## 七、相关文档

- `README.md`：运行方式、接口和主要产品行为。
- `AGENTS.md`：已确认且需要长期遵守的产品决策。
- `简历AI方法论_v0.1.md`：方法论来源、规则、边界和验收样例。
- `design-qa.md`：历次实现、浏览器检查、真实服务验证和测试记录。
