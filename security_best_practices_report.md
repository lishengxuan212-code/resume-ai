# 简历网站对外发布安全审查方案与当前评估

> 历史状态说明：本文保存的是 2026-09-24 安全改造前的预审快照。此后仓库已加入邀请码会话、请求限流、并发队列、费用止损、解析子进程、安全响应头与隐私告知。本文中的“当前”“尚未实现”和阿里云规划仅描述当日检查结果，不能作为现版本的发布结论；正式上线前仍需基于最新提交和最终部署配置重新复审。

审查日期：2026-09-24  
范围：当前 `main` 工作区的 React 前端、Express API、PDF/DOCX 处理、PDF 导出、静态 Worker 与生产依赖。阿里云规划不视为已部署事实；云平台、反向代理、域名、TLS、WAF、密钥管理、网络 ACL 和日志配置须在部署信息提供后另行验证。

## 信息可信度与审查边界

- **用户提供的规划信息（尚非线上配置证据）：** 计划部署至阿里云；首版拟使用邀请码；预计日活 1,000–2,000；外部处理预算 100 元/日。上线前确认数字口径、流量峰值与负责人。
- **仓库可复核事实：** API 当前没有邀请码校验、用户认证、应用层限流或并发队列；上传采用 Multer 内存存储；静态 Worker 不承载 API；`server/index.js` 默认只监听 `127.0.0.1`。因此邀请码只是计划中的成本闸门，不是现有控制。
- **历史数据（本次未能独立复测）：** 用户提供的 91 次外部请求共 589,550 tokens、人民币 0.88 元，均值约 0.00967 元/请求。此均值不是请求成本上限，也不能单独证明流量、重试和模型价格稳定。
- **本次动态审计限制：** 当前环境无法连接 npm audit endpoint；锁文件版本可本地核实。依赖漏洞清单以 2026-09-24 扫描为历史证据，发布前须在有网络的 CI/构建环境重新执行依赖审计。

## 结论

**目前不具备公网发布条件。** 代码侧门槛未满足：公网 API 前没有应用层身份/邀请码闸门、限流和并发配额；不可信文件在 API 主进程中解析；生产 API 部署拓扑未定义。静态 Worker 只服务资源，Express 默认监听回环地址，单独发布静态 Worker 会令核心 API 不可用。阿里云运行时、TLS/域名、入口 ACL/WAF、代理信任边界、密钥注入、跨域与安全头、脱敏日志和告警也尚未验证。

本文件是基于仓库与用户提供材料的预审，不等同于渗透测试、合规意见或阿里云配置验收。

未发现被 Git 跟踪的 API 密钥，也没有发现用户输入直接写入 `innerHTML` 或 `dangerouslySetInnerHTML` 的路径。这些是正面控制，但不构成可上线结论。

## 已确认问题

### SR-001：匿名高成本接口没有速率、并发或身份控制

- **严重性：高**
- **位置：** `server/app.js:64-137`
- **证据：** `/api/extract`、`/api/diagnose`、`/api/optimize`、`/api/export` 均可匿名 POST；应用初始化处没有认证、速率限制、并发队列或按来源配额中间件（`server/app.js:37-39`）。
- **影响：** 任何人或脚本都可批量上传文件、触发最多三次外部生成请求并占用 PDF 渲染。结果是密钥额度被耗尽、运行成本失控、正常用户被阻塞。
- **修复：** 在 API 网关或 Express 层按经过信任边界校验的客户端 IP 与邀请码/会话配额实施限流；邀请码须由服务端校验，存储哈希、支持过期/撤销和额度。为解析、生成、导出分别设置并发上限、队列等待时限和全局每日外部费用断路器。验证码只能提高自动化成本，不能替代配额与费用断路器。日志只记录请求 ID、路由、结果类别、耗时、计费量和必要的源地址摘要，不记录简历正文、提示词、完整邀请码或密钥。

### SR-002：`pdfjs-dist` 版本受高危安全公告影响，项目利用路径待核实

- **严重性：高（上线前必须升级并复测）**
- **位置：** `package-lock.json:3198-3199`、`server/extract-document.js:4,94-105`、`src/ResumePreview.jsx:3,8-10`
- **证据：** 锁定版本为 `pdfjs-dist@5.7.284`。[GitHub Security Advisory GHSA-hq66-cqwq-w95j](https://github.com/advisories/GHSA-hq66-cqwq-w95j) 标记 `>=5.6.83 <6.2.108` 为受影响版本，修复版为 `6.2.108`。本次无法连 npm audit endpoint，未重新确认完整审计清单。
- **影响：** 公告描述的任意 JavaScript 执行要求 PDF.js 打开恶意 PDF 时启用脚本（默认设置）且宿主缺少阻止脚本的 CSP。当前应用服务端解析上传 PDF，前端使用 PDF.js 加载本应用生成的预览；本次未证实原始上传 PDF 会在浏览器 PDF.js viewer 中打开，也未复现利用链。因此这是**直接依赖命中公告、可达性仍需验证**，不是已确认的本项目远程代码执行。
- **修复：** 升级到 `6.2.108` 或更高且兼容版本；确认服务端解析与浏览器预览兼容；浏览器加载 PDF 时明确关闭 scripting（若该路径支持），并部署 CSP 作为纵深控制；重新运行依赖扫描及 PDF 提取、预览、导出回归。升级前不得标记关闭。

### SR-003：不可信文档在主进程内存中解析，缺少资源隔离

- **严重性：高**
- **位置：** `server/app.js:39,64-70`、`server/document-validation.js:3,22-38`、`server/extract-document.js:94-122,124-140`、`server/extract-avatar.js:77-88`
- **证据：** Multer 使用 `memoryStorage()`；仅检查 10 MB 文件大小与 PDF/DOCX 魔数；PDF 在限制页数前已交给 PDF.js 打开；DOCX 直接交给解压/解析库；头像提取会请求 PDF 图像对象并处理像素数据。没有解析进程隔离、CPU/内存限制、解析超时或工作队列。
- **影响：** 小体积压缩炸弹、畸形 PDF、超大像素图片或高并发请求可能让单个 Node 进程内存耗尽或 CPU 饱和，导致服务拒绝响应。
- **修复：** 先落实 SR-001 的限流和并发上限；短期将解析并发设为经压测确认安全的固定值，并设置请求体、解析时长、PDF 页数/图像数量和像素上限；DOCX 限制解压后总大小和 XML 条目数量。扩大单实例容量前，将解析移至独立 worker/容器并施加 CPU、内存和墙钟时间限制。用压测和畸形文件验证后再提高配额；错误响应保持通用，确保异常路径释放缓存。

### SR-004：生产 HTTP 防护基线缺失

- **严重性：中**
- **位置：** `server/app.js:37-154`
- **证据：** 没有可见的安全响应头中间件、`app.disable('x-powered-by')`、自定义 404 处理或生产环境的统一错误记录策略。CORS 在应用中未开启，这对同源部署是安全的，但反向代理的跨域规则尚未可见。
- **影响：** 增加框架指纹、点击劫持、MIME 猜测和浏览器策略配置遗漏的风险。严重性取决于最终域名、页面是否可嵌入及边缘网关是否已设置这些头。
- **修复：** 在 Express 最前面添加经过测试的安全响应头配置，关闭 `X-Powered-By`，添加固定格式的 404 和错误处理；在 CDN/反向代理明确设置 CSP、`frame-ancestors`、`X-Content-Type-Options` 和 Referrer-Policy。若前后端跨域，按实际域名严格 allowlist，不使用通配来源与凭据组合。

### SR-005：简历内容会跨越到外部处理方，但没有可见的用户告知与数据治理

- **严重性：中（合规与信任风险）**
- **位置：** `server/providers/openai.js:7-15`、`server/providers/compatible-chat.js:5-20`、`server/prompt.js`
- **证据：** 诊断和优化将用户事实与问题发送至配置的外部端点；仓库中未找到隐私说明、同意记录、数据保留策略或删除入口。应用代码未显示本地持久化，但第三方传输仍会发生。
- **影响：** 简历通常包含姓名、联系方式、教育和工作经历。对公网用户而言，未充分告知可能引发投诉、合规及合作方审查风险。
- **修复：** 在上传和在线填写前提供准确的隐私告知与主动同意；说明处理目的、外部处理方类别、保留期限和用户删除路径；只发送完成当前任务所需字段；确保密钥位于部署平台的受控密钥管理中而非构建产物。

### SR-006：当前发布拓扑没有承载真实 API

- **严重性：高（发布阻断，但不是传统代码漏洞）**
- **位置：** `worker/index.js:1-16`、`server/index.js:13-15`、`vite.config.mjs:11-16`
- **证据：** Worker 仅从 `env.ASSETS` 返回静态资源与 SPA 回退；Express API 绑定本机回环地址；Vite 的 `/api` 代理只适用于开发环境。
- **影响：** 将该 Worker/静态目录直接公开时，前端的 `/api/*` 请求没有对应后端。若为了“让它能用”而把 Vite dev server 暴露给公网，会带来开发服务器和依赖公告的额外风险。
- **修复：** 使用独立的生产 Node 运行环境或改造为支持运行时 API 的平台函数；前端与 API 经同一受控域名和 HTTPS 网关提供；仅发布构建产物，绝不使用 `npm run dev` 或 `npm run dev:full` 作为公网服务。

## 依赖审计结果

用户提供的 `npm audit --omit=dev --json`（2026-09-24）历史结果报告 6 项：5 高危、1 中危。本次因网络受限未复核；`--omit=dev` 不代表完整构建供应链风险，应同时保存生产依赖与完整依赖两份结果，并记录 Node/npm 版本、锁文件哈希及扫描时间。

| 依赖 | 当前锁定版本 | 风险判断 |
| --- | --- | --- |
| `pdfjs-dist` | 5.7.284 | 直接处理不可信 PDF，必须在发布前升级。 |
| `vite` | 6.4.2 | 开发服务器相关高危公告；生产不得暴露 Vite，仍应升级。 |
| `browserslist` | 4.28.5 | 构建链依赖，风险主要在受攻击者控制的构建输入。 |
| `nanoid` | 3.3.15 | 传递构建依赖，当前应用未发现调用路径。 |
| `postcss` | 8.5.16 | 构建链依赖，当前应用未处理用户提供的 CSS。 |
| `baseline-browser-mapping` | 2.10.42 | 构建链依赖，中危。 |

## 正面控制与审查边界

- `.env` 和 `.env.*` 被 `.gitignore` 忽略；仓库跟踪文件扫描未发现常见私钥或 API 密钥模式。
- 外部接口地址是代码固定 allowlist，不由用户 URL 决定；未发现本项目的 SSRF 请求入口。
- PDF 导出只接受受限的 base64 图片数据 URL，未发现远程头像 URL 拉取。
- API 不使用 Cookie 或会话认证，因此经典 CSRF 不是当前主要风险；匿名 API 滥用才是主要风险。
- 未验证：云平台网络隔离、TLS、WAF、DDoS 防护、访问日志脱敏、备份、密钥轮换、异常告警和最终域名的安全响应头。

## 改造后的发布审查流程

每个门槛附配置/代码证据、验证记录、负责人和日期；“计划采用”不能作为关闭证据。任一 P0/P1 门槛未关闭时，不开放公网入口。

| 阶段 | 必须完成的检查 | 通过证据 | 阻断条件 |
| --- | --- | --- | --- |
| A. 依赖与构建 | 在线 CI 同时执行 `npm audit --omit=dev` 与完整依赖扫描；处理可修复高危项；确认构建产物不含密钥且只发布构建文件 | 锁文件、审计 JSON、构建日志和产物扫描记录 | `pdfjs-dist` 仍在公告范围；可达生产高危漏洞未评估/缓解；构建产物含密钥或开发服务配置 |
| B. API 成本闸门 | 四个 POST 路由均校验有效邀请码/会话；按 IP 和码配额；重试计入预算；每日费用断路器；解析/生成/导出并发和队列等待超时 | 无效/撤销码、超额、来源 IP 变化、并发及断路器演练记录；成本仪表盘 | 任一高成本路由匿名可调用；重试不计额度；断路器失效或可由客户端重置 |
| C. 文档解析韧性 | 验证超限文件、错误魔数、畸形 PDF/DOCX、压缩炸弹、页数/图像极值和慢解析；配置请求体、解压体积、像素、CPU/内存、时间与并发上限 | 隔离环境资源记录、错误响应及服务仍可用的压测记录 | 单份文件或小并发可令 API OOM/持续阻塞；没有时间/资源上限 |
| D. 生产拓扑与边缘 | 明确阿里云产品/网络图；仅发布构建产物；API 只从受控网关进入；验收 TLS、入口 ACL、代理信任头、CORS、Host 校验、安全头、统一错误与健康检查 | 脱敏配置证据、外网探测结果、API 端到端合成检查 | 生产 API 绕过网关可直连；暴露 Vite dev server；TLS/域名/入口权限未验收 |
| E. 密钥、隐私与日志 | 密钥由受控配置注入并验证轮换；列明外部处理方、数据字段、用途、保留期和删除方式；提交前告知/同意；日志不收正文/提示词/密钥 | 密钥扫描、轮换演练、隐私文案、日志抽样与数据流清单 | 浏览器或日志出现密钥/简历正文；用户未被告知数据将发送给外部处理方 |
| F. 运行准备 | 配置费用/错误率/延迟/RSS/事件循环/队列告警、值班责任、断路器回退和停服步骤；在真实部署验证 API 与限额 | 告警演练、值班人与回滚步骤、上线验收记录 | 无人接收预算告警；无法暂停新任务或撤销邀请码 |

## 邀请码与预算校准（待运营确认）

邀请码不是限流器：可被转发、共享或复用。服务端存储码的哈希，支持过期/撤销、每日额度，并同时按码、IP 和全局限额。共享码下无法准确区分“用户”，因此在没有账号/独立码的首版只能称为码级配额，不能称为个人配额。

依历史样本均值 0.00967 元/外部请求，单用户两次请求约 0.01934 元；诊断和生成各最多三次时最多 6 次、约 0.05802 元。按 1,000–2,000 个用户且每人一个两请求流程，均值估算 19.34–38.68 元/日；全按六次估算 58.02–116.04 元/日。此为情景计算，不是费用上界或 SLO，未涵盖模型价差、token 分布尾部、失败请求计费、PDF 和实例费用、重复流程。

每日 80 元硬上限、60 元告警可以作为待确认的保护性初值；须验证成本数据来源是否足够及时，若账单不能近实时归集，还需按码实施请求/token 预算。每码每天 2 个流程、每 IP 每小时 6 次写请求、生成与 PDF 导出并发各 4 个只能作为压测起点，不可视为已验证容量；按码/IP/全局三层联动并关注共享码误用与代理 IP 误伤。

**当前发布意见：暂缓公网开放。** A–F 阶段阻断项关闭并留存证据后，须做部署态复审；邀测也必须具备服务端邀请码校验、预算止损与资源限制。
